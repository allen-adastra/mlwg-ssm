[project]
name = "mlwg"
version = "0.1.0"
description = "Tutorial: Making Real Machines Work: Modelling with States, Actions and Observations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jax[cpu]",
    "jaxtyping",
    "matplotlib",
    "jupyter",
    "numpy",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
    "ruff>=0.15.10",
]
